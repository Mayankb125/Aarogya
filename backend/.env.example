PORT=5000
CLIENT_URL=http://localhost:5173
# Firebase Admin SDK service account.
# Dev with no Firebase project: leave this pointing at a non-existent file
# and Queue Cure runs in-memory + skips backend auth (Phase 1 + 2 demo).
FIREBASE_SERVICE_ACCOUNT=./serviceAccountKey.json
